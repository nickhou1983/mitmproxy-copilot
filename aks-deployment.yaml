# Generated by Copilot
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mitmproxy-deployment-ha
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mitmproxy-ha
  template:
    metadata:
      labels:
        app: mitmproxy-ha
    spec:
      containers:
      - name: mitmproxy-ha
        image: democrhk.azurecr.io/mitmproxy-allow-hosts:v1
        ports:
        - containerPort: 8080
        volumeMounts:
        - name: mitmproxy-app-volume
          mountPath: /app  # 挂载路径
        - name: mitmproxy-certs-volume
          mountPath: /opt/mitmproxy  # 挂载路径
      volumes:
      - name: mitmproxy-app-volume
        persistentVolumeClaim:
          claimName: mitmproxy-app-pvc-01  # PVC 名称
      - name: mitmproxy-certs-volume
        persistentVolumeClaim:
          claimName: mitmproxy-certs-pvc-01  # PVC 名称
          